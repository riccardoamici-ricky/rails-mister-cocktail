<div class="container">
  <strong><h1 class="title">CHOOSE YOUR WEAPON</h1></strong>
  <p>“Hear no evil, speak no evil, and you won't be invited to cocktail parties.”</p>
  <div class="cards">
    <% @cocktails.each do |cocktail| %>
      <div class="card-category" style="
      <% if cocktail.photo.attached? %>
        background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)),
        url('<%= cl_image_path cocktail.photo.key, class: "idd", height: 180, width: 400, crop: :fill; %>')
      <% end %>">
       <h1><%= link_to cocktail.name.upcase, cocktail_path(cocktail) %></h1>
      </div>
    <% end %>
  </div>
  <%= link_to 'NEW', new_cocktail_path, class: "btn-landing" %> </divA>
</div>
